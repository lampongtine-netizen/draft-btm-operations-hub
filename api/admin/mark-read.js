const {SUPABASE_URL,sbHeaders,staffActive}=require('../_lib/common');
module.exports=async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});if(!await staffActive(req))return res.status(401).json({error:'Staff authentication required'});try{let b=req.body;if(typeof b==='string')b=JSON.parse(b);if(!b?.conversationId)return res.status(400).json({error:'conversationId required'});const r=await fetch(`${SUPABASE_URL}/rest/v1/conversations?id=eq.${encodeURIComponent(b.conversationId)}`,{method:'PATCH',headers:sbHeaders(),body:JSON.stringify({unread_for_admin:false})});if(!r.ok)throw new Error('Failed to mark conversation as read');return res.status(200).json({ok:true});}catch(e){return res.status(500).json({error:e.message||String(e)})}};
