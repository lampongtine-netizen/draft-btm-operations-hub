const {SUPABASE_URL,sbHeaders,demoActive}=require('../_lib/common');
module.exports=async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});if(!demoActive(req))return res.status(401).json({error:'Unauthorized'});try{let b=req.body;if(typeof b==='string')b=JSON.parse(b);if(!b?.conversationId)return res.status(400).json({error:'conversationId required'});await fetch(`${SUPABASE_URL}/rest/v1/conversations?id=eq.${encodeURIComponent(b.conversationId)}`,{method:'PATCH',headers:sbHeaders(),body:JSON.stringify({unread_for_admin:false})});return res.status(200).json({ok:true});}catch(e){return res.status(500).json({error:e.message||String(e)})}};
